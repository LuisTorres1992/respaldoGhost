routes:

collections:
  /:
    permalink: /{slug}/
    template: index
    filter: 'tag:-en'
  /es/:
    permalink: /es/{slug}/
    template: index1
    filter: 'tag:es'

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/